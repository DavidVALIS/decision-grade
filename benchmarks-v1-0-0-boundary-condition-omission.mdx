---
title: 'Can it notice what is missing?'
description: 'Boundary condition omission. 500 cases. All three frontier models below 0.41 macro F1. The hard problem in AI is not lying. It is silence.'
---

This is the test.

<BenchmarkTestSection wedge="boundary_condition_omission" name="The numbers" scoreLine="Macro F1 · 500 cases · 3 models">

Across 500 cases, Claude Opus 4.7 scored macro F1 0.40. GPT-5.5 scored 0.32. Gemini 3.1 Pro scored 0.32. Three frontier reasoning models, three independent providers, three different training runs. All three score below 0.41 on detecting what is not there.

The wedge gives the model an analysis that is internally coherent and appears complete. Somewhere in that analysis, a boundary condition that a careful reader would catch has been omitted. The cases are constructed so that the omission is detectable from the text alone. No outside knowledge required. We ask the model to surface the omission. We score against the constructed-truth set.

Recall on the truth set sits at 0.43 to 0.55. The model catches roughly half of the omissions that exist. Precision is worse: when the model does flag something, it is right about 25 to 34 percent of the time. The model guesses at omissions that are not actually missing.

This holds across providers. It holds across model architectures. It holds when we vary the wedge's case difficulty. The finding survives the spot-check panel.

What we are saying. Frontier reasoning models are good at evaluating what was said. They are not good at noticing what was not said. If you are using a model to vet an argument for completeness, you should expect it to miss roughly half of the things a careful human would catch, and to flag spurious things at a similar rate.

</BenchmarkTestSection>

## What "below 95 percent" actually means

<BenchmarkExampleTable
  ceo="You read a flawless brief on entering Vietnam. It does not mention that the local labor regulations make your unit economics impossible. The brief is not wrong. It is incomplete in exactly the way that costs you $80M and eighteen months. You learn the gap on the ground."
  analyst="The AI produces a clean SWOT analysis. You ship it. Two weeks later in a partner meeting someone asks about a regulatory factor you never heard of. It was never in your sources. The AI did not flag it because the AI does not know what it does not know, and at 0.40 neither do you."
  operator="Thirty agents producing decision-grade memos with a 40 percent ceiling on noticing omissions means the archive is, structurally, a record of partial pictures. Every position is taken on incomplete grounds. The principal cannot tell which gaps matter until one of them breaks something."
/>

## What would falsify this finding

Any of the three frontier models hitting macro F1 above 0.60 at this case construction. None have at v1.0.0.

A natural objection is that we used a bare, one-shot prompt. A senior analyst with a year of experience using these models would not. They would use chain-of-thought scaffolding, self-critique passes, role priming, and structured output guidance. Whether sophisticated prompting closes a 30-point gap is itself an open question. If you build or buy AI to flag what is missing from an analysis, the answer matters.

If the implications interest you, get in touch.

## Anchored artifacts

- [manifest.yaml](https://github.com/valisbench/benchmarks/blob/main/v1.0.0/boundary_condition_omission/manifest.yaml)
- [manifest.bundle.json](https://github.com/valisbench/benchmarks/blob/main/v1.0.0/boundary_condition_omission/manifest.bundle.json)
- [manifest.yaml.ots](https://github.com/valisbench/benchmarks/blob/main/v1.0.0/boundary_condition_omission/manifest.yaml.ots)
- [cases.jsonl](https://github.com/valisbench/benchmarks/blob/main/v1.0.0/boundary_condition_omission/cases.jsonl)
- [results.jsonl](https://github.com/valisbench/benchmarks/blob/main/v1.0.0/boundary_condition_omission/results.jsonl)
